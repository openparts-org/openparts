//! OpenParts workspace root crate.
//!
//! Intentionally empty. It exists only so that `tests/` at the workspace
//! root is compiled and run by `cargo test --workspace`. All real
//! functionality lives in the crates under `crates/`.
